# Hugo
/public/
/resources/_gen/
.hugo_build.lock

# Raw design source files (Illustrator/InDesign/PDF) — kept on disk, not in repo.
# Curated, web-ready exports live under content/work/<project>/images/.
2026/

# OS
.DS_Store

# Brainstorm companion (if used)
.superpowers/

# Design docs / specs / plans — kept local, not in repo
docs/
